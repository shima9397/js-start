//5- Create a function to accept a string and return the size of it.

let getSize = (myString) => {
  return myString.length;
};

console.log(`myString size is: ${getSize("shima")}`);
console.log(`myString size is: ${getSize("if you never try you never know")}`);
